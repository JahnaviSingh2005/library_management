# 📚 Library Management System API

A RESTful API for managing library books, built with Node.js, Express, and MongoDB.

## Features
- CRUD operations for Books
- Search by Title
- Global Error Handling
- Mongoose Validations
- Environment variable configuration

## Setup
1. Clone the repository
2. Run `npm install`
3. Configure `.env` with your `MONGO_URI`
4. Run `npm run dev` for development mode
5. Run `npm start` for production mode

## Documentation
- [Technical Documentation](documentation.md)
- [Viva Preparation Guide](viva_prep.md)